# BMFC Club Hub — Supabase CLI (edge function deploy only)
#
# Schema/migrations live in supabase-club/migrations/ (apply separately).
# Edge function source of truth: supabase-club/functions/

[functions.send-push]
verify_jwt = false
entrypoint = "../supabase-club/functions/send-push/index.ts"
